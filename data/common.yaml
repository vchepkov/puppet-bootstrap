---
lookup_options:
  bootstrap::mco::admins:
    merge: unique

chrony::servers:
  - 0.centos.pool.ntp.org
  - 1.centos.pool.ntp.org
  - 2.centos.pool.ntp.org
  - 3.centos.pool.ntp.org
  
classes:
  - bootstrap::agent

#TODO: remove once bug #15865 fixed
postgresql::server::package_ensure: '9.6.13'
postgresql::server::contrib::package_ensure: "%{lookup('postgresql::server::package_ensure')}"
